# Funding is NOT enabled. Everything below is commented out on purpose.
#
# Uncommenting any line here turns on the "Sponsor" button on the repository
# page. That is a deliberate decision about how the project presents itself,
# not a default — a sponsor button on a young project with no support
# commitment invites an expectation the project has not agreed to.
#
# To enable: uncomment exactly the platforms that are actually set up and
# verified, and delete the rest. A button pointing at an account that does not
# accept sponsorship is worse than no button.
#
# github: [<username>]
# ko_fi: <username>
# buy_me_a_coffee: <username>
# open_collective: <slug>
# custom: ["https://<your-donation-url>"]
