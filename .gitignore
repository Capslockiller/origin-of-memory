# .NET
bin/
obj/
*.user
.vs/
TestResults/
publish/

# lane INT-2 synthetic end-to-end proof (never committed)
.e2e/

# bench (Python measurement tools, not shipped)
__pycache__/
*.pyc
.pytest_cache/
bench/.data/
bench/.versions/
bench/.out/
bench/.out-*/
bench/.e2e/

# local state never belongs in the repo
.state/
.oom/backup/
*.db
*.lock

# lane VM kapi 7 (Windows Sandbox) - kanit ve host'a ozel uretilen dosyalar
bench/vm/.out/
bench/vm/host.txt

# scrub/report scratch and local nuget/test artifacts (never committed)
.brief/
.nuget/
*.trx
