# v2.1.0 — 2026-09-11

See CHANGELOG.md `[2.1.0]`. Executable: `publish/win-x64/Oom.exe` (self-contained win-x64, no side files). Install: `Oom.exe --vault <vault> install`; a vault that already carries compiled knowledge is adopted with `install --adopt`. Evidence: `bench/results/`, `docs/scars.md`.
